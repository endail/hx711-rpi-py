
build-backend = "setuptools.build_meta"

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11>=2.7.0",
]

[tool.cibuildwheel]
before-all = [

    "ls -al",
    "chmod +x install-deps.sh",
    "./install-deps"

]

